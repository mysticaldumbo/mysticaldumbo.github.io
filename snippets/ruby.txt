def sum_of_squares(n)
    # Calculate the sum of squares of first n natural numbers
    (1..n).inject(0) { |sum, x| sum + x**2 }
end

puts "Sum of squares of first 5 natural numbers is: #{sum_of_squares(5)}"