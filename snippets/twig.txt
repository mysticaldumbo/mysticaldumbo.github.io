<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"> {# Define the character encoding #}
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> {# Configure viewport for responsive design #}
    <title>Ban Co.</title> {# Set the title of the page #}
    <link rel="stylesheet" href="styles.css"> {# Link to an external CSS stylesheet #}
</head>
<body>
    <header>
        <h1>Welcome to Ban Co.</h1> {# Main heading #}
    </header>
    
    <main>
        <section>
            <h2>Items</h2> {# Subheading #}
            <ul>
                {% for item in items %} {# Loop through each item in the items array #}
                    <li>{{ item.name }} - {{ item.price }}</li> {# Display item name and price #}
                {% endfor %}
            </ul>
        </section>
    </main>
    
    <footer>
        <p>&copy; {{ current_year }} My Website</p> {# Display current year and website name #}
    </footer>
</body>
</html>