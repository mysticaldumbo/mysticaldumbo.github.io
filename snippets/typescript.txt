class Circle {
    radius: number;

    constructor(radius: number) {
        this.radius = radius;
    }

    area(): number {
        // Calculate the area of the circle
        return Math.PI * this.radius * this.radius;
    }
}

const circle = new Circle(5);
console.log("Area of the circle with radius 5 is:", circle.area());